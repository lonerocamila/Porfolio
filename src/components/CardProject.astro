---
export interface Props{
    title: string;
    description: string;
    image: string;
    link: string;
    linkText: string;
}
const { title, description, image, link, linkText } = Astro.props;
---

